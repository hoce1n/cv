"use client";
import { useActionState } from "react";
import { login } from "../actions";
export default function LoginPage() { const [error, action, pending] = useActionState(login, undefined); return <main className="min-h-screen grid place-items-center bg-muted/30 p-6"><form action={action} className="w-full max-w-sm rounded-xl border bg-card p-8 shadow-sm"><h1 className="text-2xl font-semibold">Admin access</h1><p className="mt-2 text-sm text-muted-foreground">Enter the configured admin password.</p><label className="mt-6 block text-sm font-medium" htmlFor="password">Password</label><input className="mt-2 w-full rounded-md border bg-background px-3 py-2" id="password" name="password" type="password" required autoFocus />{error && <p className="mt-2 text-sm text-destructive">{error}</p>}<button className="mt-6 w-full rounded-md bg-primary px-4 py-2 text-primary-foreground disabled:opacity-50" disabled={pending}>{pending ? "Checking…" : "Sign in"}</button></form></main> }
